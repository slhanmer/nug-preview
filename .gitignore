# nug — a single mock, cut out of the studio template to be deployed.
#
# demos/ and public/media ARE committed here, which is the whole point: Vercel
# builds from git, so a mock whose content is ignored deploys as a page of
# broken images. This repo is private, it holds one business, and it gets
# deleted if they say no.
#
# public/media is written by `pnpm seed nug` — run that BEFORE the first
# commit or the deployment will have no pictures.

/node_modules
/.pnp
.pnp.*
/coverage
/.next/
/out/
/build
.DS_Store
*.pem
.env*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts
/storybook-static
_incoming
_to_delete
_shots
_sync
*storybook.log
**/__screenshots__
